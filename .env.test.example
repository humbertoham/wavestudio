# Safe local-test template. Copy to .env.test if your local test runner needs it.
# This file contains fake values only and must never be used for sandbox,
# production, UAT, or a shared database.
NODE_ENV="test"
DATABASE_URL="postgresql://test_user:test_password@127.0.0.1:5432/wavestudio_test"
WELLHUB_CHECKIN_ENABLED="true"
WELLHUB_API_BASE_URL="https://apitesting.partners.gympass.com/access/v1"
WELLHUB_API_TOKEN="fake-wellhub-test-token"
WELLHUB_GYM_ID="123456"
WELLHUB_WEBHOOK_SECRET="fake-wellhub-test-webhook-secret"
WELLHUB_API_TIMEOUT_MS="800"
